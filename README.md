# Custodia — distributed digital evidence

Custodia is a runnable forensic evidence and chain-of-custody platform built with **Python/FastAPI and React**. It stores files as SHA-256-addressed chunks across isolated storage nodes, reads through a majority quorum, preserves immutable versions, and repairs modified or missing replicas from verified peers.

![Python](https://img.shields.io/badge/Python-FastAPI-244d3d) ![React](https://img.shields.io/badge/React-TypeScript-147d70) ![Integrity](https://img.shields.io/badge/integrity-SHA--256-246b61)

## Run it

Requirements: Python 3.11 or newer and Node.js 20 or newer.

```powershell
npm.cmd run setup:python
npm.cmd install
npm.cmd run dev
```

Open <http://localhost:5173>. The first run creates a small demo registry. To run the compiled single-server application:

```powershell
npm.cmd run build
npm.cmd start
```

Then open <http://localhost:8787>. Interactive FastAPI documentation is available at <http://localhost:8787/docs>.

## Beginner guide: use Custodia from start to finish

Custodia is a dynamic local web application, not a static webpage. The React frontend provides the dashboard, the FastAPI backend performs the evidence operations, and `.data/` stores the local deployment data.

### Important terms

- **Case ID**: the investigation reference that groups related files, for example `CASE-TEST-001`.
- **Evidence ID**: the unique ID generated by Custodia after upload, for example `EV-3C4733D9`.
- **Hash**: a digital fingerprint used to detect file changes.
- **Replica**: a copy of a chunk stored on another node.
- **Quorum**: the minimum number of matching replicas needed for a trusted read. This deployment requires 2 of 3 replicas.

### Complete evidence workflow

1. Start the application with `npm.cmd run dev` and open <http://localhost:5173>.
2. Go to **Evidence** and select **Register evidence**.
3. Choose a file, enter a Case ID, add a description and tags, then select **Hash & distribute**.
4. Custodia hashes the file, splits it into 256 KiB chunks, and stores each chunk on three of four local nodes.
5. Open the new evidence item to inspect its file hash, Merkle root, chunks, replica nodes, metadata, and version history.
6. Select **Verify integrity**. A healthy artifact shows a `healthy` result and the evidence remains `verified`.
7. Select **Download**. Custodia reconstructs the file from valid replicas and checks the whole-file hash before downloading it.
8. Select **PDF custody report** to download a formatted report containing the manifest, versions, custody events, and audit-chain status. The JSON version remains available at `/api/evidence/<evidence-id>/report`.

The same Case ID should be used for files from one investigation. Use a different Case ID for a different investigation. Case IDs are entered manually; Evidence IDs are generated automatically.

### Sample evidence files

The `sample-evidence/` directory contains fictional training files. They contain no real personal or legal information.

```text
camera-capture-04.txt       camera-capture-04-v2.txt
mobile-extract.json         firewall-session-log.csv
interview-notes-01.txt      badge-inventory.csv
router-events.log           browser-history.json
disk-image-manifest.json    gps-timeline.csv
camera-checksum.txt         hash-manifest.json
email-header-01.txt         usb-inventory.csv
cloud-export.json           examiner-notes.md
acquisition-report.html     memory-capture-summary.txt
suspicious-file-list.csv    timeline-events.csv
evidence-index.json
```

### Recommended hands-on test

Use these values for a simple first test:

| File | Case ID | Tags |
| --- | --- | --- |
| `camera-capture-04.txt` | `CASE-TEST-001` | `camera, primary` |
| `mobile-extract.json` | `CASE-TEST-001` | `mobile, extract` |
| `firewall-session-log.csv` | `CASE-TEST-002` | `network, log` |

After uploading them, verify each file, download one file, and generate a custody report. The Evidence page can search by filename, Case ID, or tag.

### Test immutable versions

1. Open `camera-capture-04.txt`.
2. Select **New version**.
3. Upload `camera-capture-04-v2.txt`.
4. Enter a note such as `Added examiner review note`.

The original remains as version 1, the new file becomes version 2, and a `VERSION_CREATED` event is added to the ledger.

### Test corruption detection and repair

1. Switch the profile to **Priya Shah (administrator)**.
2. Open any evidence item.
3. Select **Simulate corruption**.
4. Select **Verify integrity**. The result should be `degraded` and the item should show `attention`.
5. Select **Repair replicas**.
6. Verify again. The result should be `healthy`.

The invalid object is moved to `.data/nodes/<node>/quarantine/`, the valid bytes are copied from a healthy replica, and both the simulated fault and repair are recorded in the audit ledger.

### Test node availability and quorum

1. Open **Storage nodes** as Priya.
2. Mark one node **offline**.
3. Download or verify an evidence item. It should still work when two valid assigned replicas remain.
4. Mark the node **online** again.

For a failure test, mark two assigned replica nodes offline. Verification reports `degraded`; with all three assigned replicas offline it reports `unrecoverable`, and a read fails because the 2-of-3 quorum is unavailable. Repair does not write to offline nodes. Always return the nodes to **online** after the test.

### Test roles

- **Morgan Reed - investigator**: can upload, verify, repair, download, and view evidence.
- **Priya Shah - administrator**: can perform all actions, including node controls and the corruption drill.
- **James Okafor - auditor**: can verify evidence and inspect the audit ledger, but cannot upload or administer nodes.

The profile menu switches demo identities. This is a testing adapter, not real authentication.

### Inspect local storage

After an upload, metadata is saved in `.data/metadata.json`. Chunk bytes are stored by hash under paths such as:

```text
.data/nodes/node-atlas/objects/5f/<full-chunk-sha256>
.data/nodes/node-boreal/objects/5f/<full-chunk-sha256>
.data/nodes/node-cinder/objects/5f/<full-chunk-sha256>
```

The same chunk may exist on three nodes. Corrupted copies are moved to the node's `quarantine/` directory during repair.

### Optional direct API test

The Evidence ID is shown at the top of an evidence detail view. Replace `EV-3C4733D9` in this example with your own ID:

```powershell
$headers = @{ "x-user-id" = "usr-priya" }

Invoke-RestMethod `
  -Method Post `
  -Uri "http://localhost:8787/api/evidence/EV-3C4733D9/verify" `
  -Headers $headers
```

Interactive API documentation is available at <http://localhost:8787/docs>.

### Testing a multi-chunk file

The default chunk size is 256 KiB. The sample evidence files are intentionally small and normally create one chunk. To test multiple chunks, first run `npm.cmd run build`, then upload the generated source map from `dist/assets/*.js.map`; it is larger than 256 KiB and will be split across several replicas.

## What is implemented

- Evidence intake with streamed multipart upload limits and SHA-256 hashing
- Fixed-size chunking and deterministic placement on three of four isolated node stores
- Content-addressed, deduplicated objects with atomic writes
- Majority-quorum reads (two valid replicas out of three) and whole-file revalidation
- Investigator, administrator, and auditor capabilities (switchable demo identities in the profile menu)
- Append-only audit ledger where every event includes the prior event hash
- Immutable evidence versions with retrievable historical content
- Online/offline node controls and quorum behavior
- Full-replica integrity scans, corrupt-object quarantine, and automatic reconstruction
- JSON chain-of-custody report export with manifests, versions, events, and certification data
- PDF chain-of-custody report download for presentation and printing
- Responsive operational UI for evidence, storage nodes, ledger activity, and recovery drills

## Try the corruption-recovery drill

1. Use the profile menu to act as **Priya Shah (administrator)**.
2. Open any evidence artifact.
3. Select **Simulate corruption** at the bottom of the evidence view.
4. Select **Repair replicas**. Custodia locates the exact divergent node/object, quarantines it under `.data/nodes/<node>/quarantine`, and recreates it from a replica whose bytes match the immutable content hash.
5. Export the custody report or open the audit ledger to see both actions committed to the hash chain.

## Storage and consistency model

For each upload, the service computes a whole-file hash, splits the bytes into 256 KiB chunks, computes a SHA-256 address for each chunk, and calculates a Merkle root over the ordered chunk hashes. Placement starts at a node derived from the content hash and walks the node ring until the replication factor is met.

A normal read requires a majority of the assigned replicas to be available and independently match the expected hash. A repair needs at least one hash-valid source: divergent bytes are quarantined, then the correct bytes are atomically written back. The reconstructed file must also match the version's whole-file hash before being released.

Local deployment data lives in `.data/`:

```text
.data/
├── metadata.json             # evidence manifests and hash-chained ledger
└── nodes/
    ├── node-atlas/objects/   # independent content-addressed object store
    ├── node-boreal/objects/
    ├── node-cinder/objects/
    └── node-delta/objects/
```

## API highlights

| Method | Route | Capability |
| --- | --- | --- |
| `POST` | `/api/evidence` | Hash, chunk, distribute, and register evidence |
| `POST` | `/api/evidence/:id/versions` | Create an immutable version |
| `GET` | `/api/evidence/:id/download` | Quorum read and verified reconstruction |
| `POST` | `/api/evidence/:id/verify` | Scan every assigned replica |
| `POST` | `/api/evidence/:id/repair` | Quarantine and reconstruct bad replicas |
| `GET` | `/api/evidence/:id/report` | Generate a custody report |
| `GET` | `/api/evidence/:id/report.pdf` | Download a formatted PDF custody report |
| `GET` | `/api/audit` | Validate and return the custody hash chain |
| `PATCH` | `/api/nodes/:id` | Simulate node availability changes |

The demo identity is selected with the `x-user-id` header (`usr-morgan`, `usr-priya`, or `usr-james`). A production deployment should replace this adapter with OIDC/JWT validation and bind each action to a verified principal.

## Production stack path

The runnable Python edition deliberately uses filesystem and atomic-JSON adapters so it works with no external infrastructure. Its boundaries map directly to the suggested production stack:

- Replace each node directory adapter with a MinIO bucket client; keep content hashes as object keys.
- Move manifests and identity grants into PostgreSQL with serializable transactions and row-level security.
- Publish committed audit events through Kafka using an outbox table; run verification/repair as consumer workers.
- Add gRPC for node-to-node traffic while retaining FastAPI as the HTTP gateway for React.
- Sign ledger heads with a KMS-backed asymmetric key and periodically anchor them to external trusted timestamps.

Before evidentiary production use, add organization-specific retention/legal-hold policy, OIDC/MFA, KMS signing, TLS/mTLS, malware isolation, external time-stamping, secrets management, and an accredited validation process.

## Verification

```powershell
npm.cmd run setup:python
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
```

The tests cover deterministic chunk replication and quorum reconstruction, modified-replica detection and repair, audit-chain validity, version retrieval, and role capabilities.

The Python backend lives in `backend/`: `main.py` defines the FastAPI contract and `service.py` contains the storage, cryptography, quorum, versioning, and repair engine. Python tests live in `tests/`.
